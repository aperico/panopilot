# PanoPilot 0.18.3 Acceptance Gate

Status: READY FOR USER VALIDATION

Validate:
- Set Camera changes CAM 0 -> CAM 1;
- Camera Position is immediately saved without separate Ctrl+S;
- closing/reopening preserves it;
- camera-at reports count=1 and mode=hold-single;
- one Camera Position holds across the Clip;
- second Camera Position produces interpolation.
